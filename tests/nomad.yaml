keycloak:
  server_url: "http://keycloak:8080/"
  realm_name: "fairdi_nomad_test"
client:
  url: "http://localhost:8000/nomad-oasis/api"
  user: "test"
  password: "password"
services:
  upload_limit: 1000

plugins:
  entry_points:
    exclude:
      - "nomad.config.models.plugins:entry_points/options/apps/1_all/1_entries"
      - "nomad.config.models.plugins:entry_points/options/apps/2_theory/1_calculations"
